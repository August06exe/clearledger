# SCOREBOARD — tests/v0.6（v0.5 配置工作台验收）

> 三路同权重计数：数值（W）/ 行为（B，含变异体 MUT）/ 蜕变（MR）。
> 判分前必核三个"不可被腐蚀"的数字：答案 manifest 哈希、判分器哈希、变异 kill 率。
> 失败 → 修复 → 计数清零重跑；债务单列尾部，不混进通过数。

| 轮 | 判分 | 答案 manifest 哈希 | 判分器哈希 | 变异 kill 率 | 债务 |
|---|---|---|---|---|---|
| 1 | _（评审填写）_ | _见下方密封留档_ | _（score.py SHA256，判分时钉死）_ | _（caught/(caught+survived)，闸门 3/3）_ | _（如有，按 oracle-grader 铁律单列）_ |

## 密封留档（命题方填写，判分时核对）

- **轮 1 答案密封申报哈希（manifest.sha256 自身 SHA256）：`5e3d5e8463e798bc52e5c88a99f8b5d98cd082282659a2795c940183088e3d27`**（2026-09-20，首次密封，无旧哈希档案）。重密封必须申报新哈希并保留旧哈希于此。
- 密封区内容：`answer.json`（独立 pandas 口径：pending 稳定投影 / impact 映射 / 行数对数）、`caliber.json`（容差类与假设 A1~A9）、`oracle.py`（答案计算器，纯 pandas+yaml，零被测系统 import）。
- 判分口径跨轮冻结：自轮 1 起，caliber.json 修改 = 上一轮作废重跑。
- 变异体菜单：`tests/v0.6/mutations/`（M1/M2/M3，被测方提供）；变异体只活一个轮次，测完即删副本。

## 债务清单（自曝区）

| Case | 类别 | 设计内容 | 未执行原因 | 补测计划 |
|---|---|---|---|---|
| _（空）_ | | | | |
