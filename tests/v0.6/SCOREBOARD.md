# SCOREBOARD — tests/v0.6（v0.5 配置工作台验收）

> 三路同权重计数：数值（W）/ 行为（B，含变异体 MUT）/ 蜕变（MR）。
> 判分前必核三个"不可被腐蚀"的数字：答案 manifest 哈希、判分器哈希、变异 kill 率。
> 失败 → 修复 → 计数清零重跑；债务单列尾部，不混进通过数。

| 轮 | 判分 | 答案 manifest 哈希 | 判分器哈希 | 变异 kill 率 | 债务 |
|---|---|---|---|---|---|
| 1 | 12/19 | `5e3d5e84…88e3d27`（**已作废**，见下） | _（R1 判分时钉死）_ | **1/3**（M1/M2/M3 中 2 个存活=测试盲区） | 见债务清单 |
| 2 | _（评审填写）_ | **`0d0fb81a7a10b570a431c198ec9f9d2e8fd8bbbda0bd7eacceaf32fc99655f82`** | _（score.py SHA256，判分时钉死）_ | _（M1~M6，闸门：M4/M5/M6 新血全 caught）_ | _（如有）_ |

## 密封留档（命题方填写，判分时核对）

- **R1 manifest `5e3d5e8463e798bc52e5c88a99f8b5d98cd082282659a2795c940183088e3d27` 已作废**（2026-09-21 R1 归因裁定触发语义变更：两阶段校验 v2 / type 行保留 / required 行摄取层剔除 / raw 行数口径改判 / 匹配契约行 cnt=distinct 键数）。旧哈希档案留存本表，不得再用其判分。
- **R2 答案密封申报哈希（manifest.sha256 自身 SHA256）：`0d0fb81a7a10b570a431c198ec9f9d2e8fd8bbbda0bd7eacceaf32fc99655f82`**（2026-09-21）。重密封必须申报新哈希并保留旧哈希于此。
- 密封区内容：`answer.json`（v2：pending 稳定投影含匹配契约行、impact 映射、行数对数）、`caliber.json`（v2 容差类 + 假设 A1~A9v2 + R1→R2 变更日志）、`oracle.py`（v2 答案计算器，纯 pandas+yaml，零被测系统 import，可复算验证）。
- 判分口径跨轮冻结：自轮 1 起，caliber.json 修改 = 上一轮作废重跑（R2 的 caliber 变更本身即 R1 裁定的落地，属裁定执行非口径漂移）。
- 变异体菜单：`tests/v0.6/mutations/`（M1/M2/M3 为 R1 血；**M4 备份跳过类 / M5 别名层清空类 / M6 查询白名单旁路类** 为 R2 换血新血，bug 类声明与判据见 TESTPLAN §7，补丁由被测方按 `MUTATIONS.json` 条目实现）；变异体只活一个轮次，测完即删副本。
- R2 命题加深（密封外 Case，期望为结构/零偏差断言）：W-04 分层勾稽（ladder L3/L4/L5 vs 台账直算）、W-05 字段级血缘（sales stg + ladder 级联 mart 全列 ok=true）、B-09 变异服务器身份断言、MR-05 分层级联 save+rebuild 行集守恒。

## 债务清单（自曝区）

| Case | 类别 | 设计内容 | 未执行原因 | 补测计划 |
|---|---|---|---|---|
| R1-MUT-M2 | 变异体存活 | 挂起队列丢级（漏 level 行） | R1 装备 2 变异体存活（评审归因）；R2 已按裁定换血并重写判据（distinct 键数 + 幽灵行 V10 加深判别） | R2 判分核对 |
| R1-B-05 | 工序缺陷 | 写向穿越探针编码形态不可达 | R2 已改单段 `..` 实例名 + `--path-as-is` 实测可达形态 | R2 判分核对 |
| R1-MUT-启动 | 工序缺陷 | `../../../.venv` 从副本目录解析越界 | R2 已改仓库根绝对解释器路径 + 8630 预清理 + B-09 身份断言 | R2 判分核对 |
