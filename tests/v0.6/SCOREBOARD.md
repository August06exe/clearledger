# SCOREBOARD — tests/v0.6（v0.5 配置工作台验收）

> 三路同权重计数：数值（W）/ 行为（B，含变异体 MUT）/ 蜕变（MR）。
> 判分前必核三个"不可被腐蚀"的数字：答案 manifest 哈希、判分器哈希、变异 kill 率。
> 失败 → 修复 → 计数清零重跑；债务单列尾部，不混进通过数。

| 轮 | 判分 | 答案 manifest 哈希 | 判分器哈希 | 变异 kill 率 | 债务 |
|---|---|---|---|---|---|
| 1 | 12/19 | `5e3d5e84…88e3d27`（**已作废**，见下） | `bac118cf06f8a1f4aeb86f675f50997ddef8580a3376a51347e8e36bac935976` | **1/3**（M1/M3 存活=装备等价变异；M2 caught） | 见债务清单 |
| 2 | **22/28** | **`0d0fb81a7a10b570a431c198ec9f9d2e8fd8bbbda0bd7eacceaf32fc99655f82`** | `ab55d8c29471373c9cf26543e9d9ef1b7ecf2db3e2b1e96cecbee5ef7b30eed0` | **5/6**（M1/M2/M3/M4/M5 caught；M6 存活记债务；闸门"M4/M5/M6 新血全 caught"未达） | 见债务清单 |
| 3 | **26/28**（非 PASS 仅 B-05、B-08，均非引擎缺陷） | **`3114f85b190e4567d267b43b817f00217f4a386b4c0cb7c69f1facc8fdb55989`**（manifest_verified=true 逐条核验通过） | `340872fbdc2e5d3617b433c35369effdb960b554c6a88d0293492cf1009fb062` | **6/6 = 100%**（M6 按 R2 裁定 E 三重判据 caught；变异债务清零） | 0（变异债务清零；B-05/B-08 见债务清单注记） |

**闸门状态（评审裁定）**：R1 12/19 ✗ → R2 22/28 ✗ → R3 26/28 ✗ → 闸门计数 **0/3**。R4 于当前干净态全绿则 1/3（命题加深可选；加深须换血 ≥50% 变异体并重密封申报）；R5 回归全绿 = 2/3。

判分器演进备注：注册表随 R2 密封扩至 28 case（+W-04/W-05、B-09~B-11、MR-05、MUT-M4~M6）；R2 判分中评审修正一处判分器自身缺陷（W-03 的 `rows.files.*` 在 A5v2 下不再等于 raw 表行数，改为直接清点 inbox 源文件数据行多重集——修正仅消除 R1 口径残留的 files 误报，W-03 其余失败不变）；修正后钉死哈希即上表 R2 值，`score_round2.json` 以该版出具。R3 判分修正两处：① mr05 哈希行比较缺陷（此前误把 sha256sum 行尾 before/after 标签纳入比较，现只比哈希 token）；② `_in` 键 observed 值语义澄清（单请求单观察：标量或单元素列表等价可收；多元素列表=回抄词表非观察，判 `multi_value_observation_for_single_request`）。R3 判分以修正后哈希（上表 R3 值）出具 `score_round3.json`。

## 密封留档（命题方填写，判分时核对）

- **R3 答案密封申报哈希（manifest.sha256 自身 SHA256）：`3114f85b190e4567d267b43b817f00217f4a386b4c0cb7c69f1facc8fdb55989`**（2026-09-21，R2 判分四条裁定后重密封）。重密封必须申报新哈希并保留旧哈希于此。
- **作废档案**：
  - R1 `5e3d5e8463e798bc52e5c88a99f8b5d98cd082282659a2795c940183088e3d27`（R1 归因裁定触发语义变更：两阶段校验 v2 / type 行保留 / required 行摄取层剔除 / raw 行数口径改判 / 匹配契约行收获）；
  - R2 `0d0fb81a7a10b570a431c198ec9f9d2e8fd8bbbda0bd7eacceaf32fc99655f82`（R2 判分裁定触发 null_match 口径对齐：空键属 missing/required 语义覆盖，不属 null_match）。两者不得再用于判分。
- 密封区内容（v3）：`answer.json`（pending 稳定投影 8 行——6 条字段行与 fanout 与 R2 实测一致，**null_match cnt 由 2 改 1** 为 R3 唯一数值变更；impact 映射；行数对数，`wide_ledger` 键名为语义标签，物化名 `intermediate.int_wide_ledger`）、`caliber.json`（A1~A9v3 + R1→R2→R3 变更日志）、`oracle.py`（v3 计算器，纯 pandas+yaml，零被测系统 import，可复算验证）。
- 判分口径跨轮冻结：自轮 1 起，caliber.json 修改 = 上一轮作废重跑（R2/R3 的 caliber 变更均为判分裁定的落地执行，属裁定对齐非口径漂移）。
- 变异体菜单：`tests/v0.6/mutations/`（M1/M2/M3 为 R1 血，R2 已补注入位清偿；M4 备份跳过类 / M5 别名层清空类 / M6 查询白名单旁路类 为 R2 换血新血）；R3 起 M6 探针 = 合法维度 KEY + 非法 VALUE + 合法值对照组 + 副本保真度前置（TESTPLAN §7/B-11，R2 裁定 E）。
- 引擎配套（R2 起）：`semantic.harvest --instance <账套>` 收获 CLI——基线链、基线恢复、MR-03 均含此步；W-01 采集必须在 harvest 之后（TESTPLAN §2 已明文）。
- R2 密封外 Case（结构/零偏差断言）：W-04 分层勾稽、W-05 字段级血缘、B-09 变异身份+副本保真度、B-10 别名层、B-11 白名单回退（R3 加合法值对照组）、MR-05 分层级联守恒。

## 债务清单（自曝区）

| Case | 类别 | 设计内容 | 状态/未执行原因 | 补测计划 |
|---|---|---|---|---|
| R1-MUT-M1 | 变异体存活（装备） | 保存绕过校验（save 不校验直接写） | **已清偿（R2）**：M1 变异后 syntax 探针 200 落盘=caught（R1 系等价变异：注入位点非唯一守门点，实现重构后失效） | — |
| R1-MUT-M3 | 变异体存活（装备） | 白名单/防穿越失效（任意块/路径可读写） | **已清偿（R2）**：MUTATIONS.json 补第二注入位（old2/new2 连块白名单一并移除）后 caught | — |
| R1-MUT-启动 | 工序缺陷 | `../../../.venv` 从副本目录解析越界 | **已清偿（R2）**：改仓库根绝对解释器路径 + 8630 预清理 + B-09 身份断言全过 | — |
| R1-B-05 | 命题/工序缺陷 | 写向穿越探针 URL 形态不可达（4 段路径打不进 save 路由，405） | **部分清偿（R2）**：五探针均 404 到达工作台路由，状态值全部落进 [400,404]；但 observed 键名再次偏离 expect（`_in` 后缀丢失）→ 仍 INVALID，详见 R2 报告 | **R3 结果：仍未清偿（形态三变）**——键名已逐字合规（裁定 D 落地），但 observed 值回抄词表 `[400,404]` 而非实测单值（单请求字段多值观察=非观察，判 FAIL×5）。实质行为达标（五探针 404、边界净空、根目录零改动，evidence 在案） | R4：`*_in` 键填实测单值（如 `404`），词表只属于 expect |
| **R2-MUT-M6** | 变异体存活（装备） | 查询白名单旁路（非法筛选值直接进查询，D14 回退失效） | **已清偿（R3）**：按裁定 E 重设计探针——合法 KEY（区域）+ 非法 VALUE + 合法值对照组（0<17<85 成立，证过滤路径活）+ 副本保真度对照（pending 85=85）；副本 badfilter=0 行 ≠ 85 行、200 → 非法值直进 SQL、D14 回退被旁路 = caught。M1~M6 全 caught，kill 6/6 | — |
| R2-B-05/B-06 | 工序缺陷 | observed 未按 expect 声明键填写（`_in` 键改标量键；B-06 丢 run_light_in） | **B-06 已清偿（R3 PASS）**：键名合规 + `["yellow"]` 单元素包装按单观察语义收下。**B-05 未清偿**：键名合规但值回抄词表（见 R1-B-05 行 R3 结果） | 见 R1-B-05 行 |
| R3-B-08 | **非缺陷裁定**（历史遗留迁移） | 官方账套哈希不变（含 ladder） | R3 实测 false=**R2 遗留 CRLF 字节态的迁移被诚实捕获**：轮初盘上为 R2 缺陷遗留 CRLF 态 `97d8c9c3`（2675B），MR-05 换行修复版原样 save 把 GET 服务序列化规范态 `27ee0432`（2602B LF）写回盘上，旧字节完整留证于 `config_history/dashboard.prev.yml`；与 R2 前 hash（27ee0432）闭环=迁移回归规范态，非新损坏 | **R4 于当前干净态复测 B-08 应 PASS**（已无遗留态可迁移）；工序注记：轮次开始时官方账套应先验规范态 |
