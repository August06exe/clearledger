# 生成物：由 semantic.compile_dbt 从 instances/hro/ 五配置生成，勿手改
name: cl_hro
version: "1.0.0"
config-version: 2
profile: cl

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
clean-targets: ["target"]

models:
  cl_hro:
    +materialized: view
    staging:
      +schema: staging
    intermediate:
      +schema: intermediate
    marts:
      +schema: marts
      +materialized: table
