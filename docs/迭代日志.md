# 迭代日志（夜航自审循环）

> 循环流程：调研 → 独立审计 agent 提出修改意见 → 确认（对照需求基线）→ 修复 → 记录。
> 每轮审计由**未参与编码的独立 agent**执行，结论原样记录，修复情况逐条标注。

## 第 0 轮：开发自测记录（2026-09-18 夜）

| 阶段 | 结果 |
|------|------|
| 数据生成 | 186,331 行流水 / 180 客户 / 24 商品 / 630 费用行；预埋异常生效（华东 2026-05 行数 -73%） |
| 摄取 | 5/5 源成功入库，load_log 留痕正常 |
| dbt build | 51 节点：50 PASS + 1 WARN（即预埋黄灯）+ 0 ERROR |
| 门户 API | overview / lineage / column-lineage / dictionary / reports / export / runs / trigger 全通 |
| 完整跑批（API 触发） | 7 秒完成，整体黄灯，counts=57（12 模型 38 测试通过 + 1 告警 + 6 源表不执行） |
| 字段级血缘 | SQLGlot 解析修复后全部 available（曾因新版 API 参数位置错误全线失败，已修复） |

（后续审计轮次见下方追加章节）

## 第 1 轮：独立双审计（代码审计 agent × 1 + 口径审计 agent × 1，互不知情）

两个 agent 共提出 27 条发现（去重后 22 条），分级处置如下。

### P0（已修复，当晚验证）

| # | 发现 | 修复 | 验证方式 |
|---|------|------|----------|
| P0-1 | dbt 崩溃未写 run_results 时误报"绿灯"（甚至归档上一轮旧结果） | 总判定改为：摄取失败 / dbt 返回码≠0 / 无本轮新产物 → 一律红灯；run_results 按 mtime 判定是否本轮产物 | 代码审查 + 逻辑推演 |
| P0-2 | "红=报表标记过期"只有后端拦截、前端无任何过期提示 | 报表 API 返回 stale + stale_info；报表页顶部红色横幅；Excel 导出在红灯时首行写警示 | 实测：移走源文件跑批 → light=red、stale=True、源节点变红；恢复后 stale=False |

### P1（已修复 7 条）

| # | 发现 | 修复 |
|---|------|------|
| P1-1 | 中文 Windows 默认 GBK，跑批日志子进程输出会乱码，毁掉自救通道 | 子进程注入 PYTHONUTF8=1 / PYTHONIOENCODING=utf-8；bat 同步设置 |
| P1-2 | 定时跑批错过即静默丢失（APScheduler 默认宽限仅 1 秒） | misfire_grace_time=6h + coalesce；启动时发现上次跑批 >24h 自动补跑（触发器标记"补跑"） |
| P1-3 | 备份脚本静默失败 + 热备份损坏风险 | 每步 errorlevel 检查、连同 .wal 一起备份、PowerShell 时间戳、失败显式报错 |
| P1-4 | 零收入月份总部费用被 inner join 蒸发，净利虚高 | 费用分摊改为月×区域骨架 + 零收入月按区域数均摊兜底 |
| P1-5 | 客户 LTM 混入不完整当月，违反自家口径文档 | LTM 窗口改为最近 12 个完整月 |
| P1-6 | 环比基于"上一有数月"，缺月即跳月；零销售区域月在报表中消失 | kpi/region 两层 marts 改为日历月骨架（零销售月补 0 行），环比永远基于上一日历月 |
| P1-7 | 折前金额盲信源 amount 列、折扣率无范围校验，口径漂移无告警 | 新增 warn 测试：金额=数量×单价校验、折扣率[0,1]、流水数值合理性、清洗剔行>1% 对账；新增 error 测试：销售事业部必须有负责区域 |

### P2（已修复 6 条 / 记录 3 条）

已修：_guard 扩展 duckdb.Error→503；manifest 并发读退回旧缓存；空文件明确中文报错；run_results 归档自动清理；依赖加版本上限；演示数据截止日期动态化（保证新鲜度演示恒绿）；血缘图隐藏孤立节点 load_log；源节点按摄取结果着色；Excel 整数列格式；前端环比改用 marts 字段（消灭第二处环比定义）；报表页 503 容错。

记录待办（不影响验收）：① 报表列格式靠中文标签启发式，扩到 20 张报表前应把 (key,label,type) 三元组配置化；② drawChart 每报表硬编码，扩容前需通用渲染；③ 字段级血缘可加"反向影响面"一键下钻。

### 第 1 轮遗留验证

- 红灯端到端：✅ 实测通过（红 → stale 横幅 → 源节点红 → 恢复 → 黄）
- 新增 5 个测试全部通过，预埋异常仍精确触发 1 个 warn（55 节点 = 12 模型 + 42 通过 + 1 告警）

## 第 2 轮：验证型审计 + 浏览器实测 + 故障注入

### 独立审计员复核（第 1 轮 7 项修复逐项验证）

- a~g 修复全部判定通过；抓到第 1 轮修复自身引入的 1 个 P1：
  - **P1**：dbt 失败但写出新鲜 run_results（最常见的"测试失败"场景）时 `nodes_out` 未初始化 → UnboundLocalError 被兜底吞掉，红灯仍亮但节点明细丢失。→ 已修复并做**故障注入实测**（放一个必失败测试跑批）：红 + 56 节点明细完整 + 报错原因明确。注入物已移除，基线恢复黄。
- P2 处置：分摊逐行舍入尾差 0.04 元（记录，v0.4 最大余量法）；月骨架改为"费用月 ∪ 销售月"兜住整月零销售、区域名改取客户维表（均已修）；3 个页面补 503 容错（已修）。

### 浏览器端到端实测（真浏览器逐页截图目检）

| 页面 | 结果 |
|------|------|
| 总览 | ✅ 红绿灯横幅/KPI 卡/双走势图/管道 chips/跑批历史（含红灯记录）全部正常 |
| 数据血缘 | ✅ 蜘蛛网图五层布局清晰；点节点出面板：描述+测试（warn 标记）+字段口径+**字段级血缘**（month ← stg_expenses.expense_date 与 int_sales_enriched.order_date 双上游正确） |
| 数据字典 | ✅ 分层表树（源/模型/日志徽章）+ 字段中文含义 + 类型 |
| 管理报表 | ✅ 5 张报表、组合图 + 明细表 + 参数区 + 导出按钮；2026-05 异常凹陷在图上肉眼可见 |
| 跑批历史 | ✅ 摄取留痕、节点明细、日志查看（DOM 验证） |

实测揪出并修复 3 个真前端 bug：

1. **状态词汇不统一**：后端给模型节点的状态是 dbt 词汇（success/pass），给源节点的是灯色词汇（green/red），前端映射表缺 green/yellow/red 键 → 图上大量节点错误显示灰色。→ 在 `/api/lineage/graph` 统一归一化为灯色词汇。
2. **G6 fitView 时序**：fitView 在布局落地前执行，血缘图被放大 411% 只见一个节点。→ 改为 afterlayout 事件后再适配视图。
3. **静态资源缓存**：升级 JS 后浏览器仍用旧缓存（对单人用户日后升级是隐患）。→ index.html 给全部静态资源加版本号参数（升级时改一处版本串即可）。

另：暴露 `window.__clGraph` 调试句柄（控制台可直接检查图实例，方便 AI 运维）。

### 结论

两轮独立审计 + 浏览器实测 + 故障注入全部闭环。剩余已记录待办（不阻塞验收）：报表列格式配置化、图表通用渲染、字段级血缘"反向影响面"下钻、分摊最大余量法——均归入 v0.3/v0.4。

## 第 3 轮：AI-Native 文档化复盘（2026-09-19，用户拍板第一性原则）

用户明确：**项目第一性要求是 AI-Native，必须有详尽到极致的文档教 AI 系统如何运转、如何被 agent 自动化**。复盘结论：代码可读性 6/10→8/10（docstring/字典/决策记录已在），但"AI 接管上下文"为 0——新增三件套补齐：

1. **AGENTS.md**（仓库根）— agent 接管第一入口：30 秒认知、7 条红线、快速命令、"被指派任务时的第一动作"
2. **docs/AI-操作手册.md** — 操作圣经：一页系统地图（含红绿灯判定逻辑与两套状态词汇）、13 项状态资产清单（谁写/含什么/可否删）、8 个操作配方（跑批/加源/加指标/改配置/备份迁移/重启/演示数据）、按症状组织的故障 playbook（红灯三分法等）、agent 自动化 SOP（巡检/红灯自愈循环/改动后固定验收序列/API 面清单/夜航范式）、历史教训 TOP8（从第 0~2 轮提炼防复发）
3. **ops/doctor.py** — 13 项自检（环境/仓库/投放区/跑批/端口/配置/产物），人类表格 + `--json` 机器可读双模式，实测全绿

**自我要求**：此后每个迭代交付物都必须包含"AI 可操作性"更新（新配方进手册、新故障进 playbook、新红线进 AGENTS.md）；v0.5 积木化时手册 §3 配方将重写为配置操作，并新增"AI 生成五份配置的 SOP"。

## 第 4 轮：MVP 全面 QA（2026-09-19，用户指令"做做测试看看有没有 bug"）

四条战线：API 边界 12 项 / 后端健壮性（重启持久化、并发）/ 前端五页 SPA 巡检+深交互（带 window.onerror 收集器）/ 故障注入。

### 抓到并修复的真 bug（2 个）

| # | 严重度 | 现象 | 根因 | 修复 |
|---|--------|------|------|------|
| QA-1 | P1 | 报表请求 `months≥17`（覆盖最早月份）直接 500 | 最早月份环比为 NULL；pandas float64 列 `.where(notna, None)` 塞不进 None（强转回 NaN），NaN 进 JSON 序列化抛 ValueError。此前未暴露是因为默认只查 12 个月（窗口内无 NULL） | `duck.df_to_dicts` 先 `astype(object)` 再置 None；回归 17/18/36 全 200 |
| QA-2 | P2 | `data/settings.json` 被手改坏（如 hour:"abc"）→ /api/settings 500 | load() 的 int() 强转未兜异常 | 逐字段 try 回退默认值；回归：坏配置 → 回退 06:30 手动 |

### 验证通过、无产品 bug 的项

- API 边界：未知报表/未知节点/未知运行记录 404 ✓；months 非数字 422 ✓；越界值钳制 ✓；region 非白名单静默忽略（记为决策 D14）✓；导出 xlsx 用 openpyxl 实际打开验证（中文 sheet/表头/行数）✓
- 并发：跑批进行中二次触发 → 409 ✓
- 设置：跨重启持久化 ✓（定时 08:05 重启后保留且 next_run_time 正确注册；测毕恢复手动默认）
- 前端：五页 SPA 巡检 + 血缘搜索/节点面板/字典搜索/报表切页改参筛选/跑批详情/设置弹窗全链路，**window.onerror 全程零 JS 错误**；报表筛选 R01→仅华东、清空→全区域 ✓
- 测试方法学备注：巡检中 4 个"失败"经三方互证（JS 原生事件路径/坐标点击/DOM 检查）确认为**测试工具限制**（后台标签页鼠标模拟 + 等待不足与异步渲染赛跑），非产品缺陷——对真实用户功能正常

## 第 5 轮：v0.3 通用积木夜航（2026-09-19，用户指令：长程任务+三Agent复杂场景测试）

### 交付

- **设计**：《语义层与多实例设计》——五配置 schema 全文、三契约、引擎架构、双实例验证策略
- **引擎**（semantic/ 四模块，零业务预设）：loader（交叉校验+expr标识符启发式）/ ingest_run（入口档案+字段契约运行时+contract_report留痕）/ compile_dbt（配置→完整dbt project，生成物进git带diff摘要）/ query（查询编译器）
- **实例一 sales**（影子迁移）：生成管道 37/37 全绿，与手写管道对数**四项绝对差全部 0.0000**、区域月表 85 行全列一致
- **实例二 restaurant**（连锁餐饮，完全不同数据形态）：42.8万行订单全链路 27/27 全绿，引擎 git diff=0
- **三 Agent 三权分立测试**：命题（只读设计出12题，不看实现）→ 测试（忠实执行）→ 评审（独立复查+盲区扫描）

### 测试战果（三权分立的价值实证）

- 测试 Agent 抓到 9 个真问题；评审 Agent 维持全部裁决并追加 10 个盲区发现（N-01 多文件遮蔽、N-02 异常零隔离等）
- **最重一发 P-01**：字段契约校验整段死代码（rename 先于 validate，全部 continue）——评审以 raw 全 VARCHAR + contract_report 零类型记录独立铁证复核
- 修复 16 项（3 必修 + 7 拍板定谳进设计文档 §7 + 6 工程债），复测全部实证（见 tests/report_v0.3.md 复测纪要）
- 回归：两实例重建全绿、对数保持 0.0000 差

### 教训新增

| # | 教训 |
|---|------|
| 9 | YAML 1.1 的 on/off/yes/no 是布尔字面量——配置键名严禁 on（本夜踩：join 键改名 keys） |
| 10 | "配置驱动"的测试必须验证**配置真的被消费**（死代码能通过一切happy path——P-01 之训） |
| 11 | pandas 3 字符串列是 StringDtype 而非 object——dtype 判断必须两种都判 |
| 12 | 相对路径（dbt profiles/logs）依赖 CWD 是定时炸弹——生成物写绝对路径 |

### 遗留（不阻断验收，v0.3 续作）

挂起队列 UI、匹空清单门户级落地、门户实例切换 UI（数据层已就绪）、missing 计数留痕、dbt_runner 实例感知（门户"立即跑批"仍走旧管道）。

## 第 6 轮：v0.4 开放接口夜航（2026-09-20 凌晨，长程任务）

### 交付

- **MCP 只读服务** `mcp_server.py`（stdio，六工具：账套/指标目录/报表列表/报表查询/数据健康/口径；审计落 `logs/mcp_audit.jsonl`；SDK 锁 `mcp>=1.2,<2` 防 2.x 破坏）
- **HTTP 开放接口** `/api/open/*`（X-API-Key 鉴权，401/403/留痕；key 模板 `openapi_keys.example.json`）
- **装配线体检器** `semantic/inspect.py`（列画像→实体猜测→join 建议→`onboarding/config_draft.yml` 五配置草案）
- 三权分立硬核测试 R1 45/48 → 修复 7 项（F1 查询截断/审计副作用、F2 run_id、F3 503→400、F4-F7）→ R2 44/48
  （剩余 2 FAIL 均为采集脚本适配问题非引擎问题，闸门未闭合，续作）

### 教训新增

| # | 教训 |
|---|------|
| 13 | MCP 工具函数名与被导入函数同名 → 自我遮蔽无限递归（import 别名解决） |
| 14 | 封闭式验收测试的密封答案 schema 要先冻结——工具输出字段名（name vs instance）中途漂移伤了一轮 |

## 第 7 轮：可用性三件套（2026-09-20，用户指令：exe/用法指南/点火文档）

### 背景

用户点破真问题：**快速开始是"人肉命令流"，与产品 AI-Native 定位自相矛盾**——配置和脚本本来就设计为 agent 领地，引导却教人敲命令；且旧 `启动明账.bat` 只建 venv 不做数据/跑批，新克隆点开是空门户。

### 交付

- **`启动明账.exe`**（PyInstaller 单文件 + tk 图形窗，`ops/launcher.py`）：自动走完 点火指南 Step1~6；「重建演示数据」按钮并入；开发态/冻结态双 `--smoke` PASS。`启动明账.bat`/`重建演示数据.bat` 退役
- **`docs/AI-点火指南.md`**：agent 自举 playbook（前置检查→六步流水线每步验证点→验收清单→用户三诉求引导→首跑故障→红线十秒版）；挂入 AGENTS.md 必读链第 2 位
- **README 快速开始重写**（双语）：方式一交给 agent（推荐，"读点火指南把我跑起来"）> 方式二双击 exe > 方式三裸命令（折叠）
- **门户首开 AI 提醒**：浅蓝横幅（可勾"下次不再提醒"，持久化 `data/settings.json` 的 `ai_banner`）+ 左侧「AI 接入」页（agent 指令模板/MCP 配置/HTTP 说明/提醒开关，带复制按钮）；静态资源升版 `?v=20260920a`
- 引用同步：doctor/验收指南/手册（新增 R-12 配方）；README.en 顺手修掉两段未清理旧叙事与 `README.zh-CN.md` 坏链（此前遗留）

### 教训新增

| # | 教训 |
|---|------|
| 15 | 产品哲学必须贯通到引导层：号称 AI-Native 却教用户敲命令，等于自我否定——"用法"也是产品的一部分 |
| 16 | 启动脚本与文档承诺要对齐：bat 只建 venv 不做数据，新克隆开箱即空门户，这种"半吊子自动化"比没有更伤信任 |

## 第 8 轮：v0.4 收官（2026-09-20 晚，用户指令：推进更新计划）

### 交付

- **界面中文化**（v0.4 遗留项）：`app/services/aliases.py` 别名层——技术名→中文全部从六份配置推导（零新增维护负担），`/api/aliases` + 前端 `App.alias()` 接入血缘/字典/跑批/总览四页，中文主显+英文小字副显+中文可搜索；静态资源升版 v=20260920b。浏览器实测：血缘图 16 节点、字典表列全部中文
- **HTTP open data 端点补 columns**（与 MCP query_report 消费面对齐，外部 agent 一致性）
- **测试闸门闭合**：修复采集脚本两处形态漂移（B-09 键名、B-08 parsed）后，R3/R4/R5 三轮**连续 48/48=100%**，全部初见杀测试 Agent 执行、--strict-manifest 密封校验通过——闸门达成，v0.4 正式收官
- 补跑 sales 三步链（37/37 全绿）修复"尚未跑批"状态（历史机制上线后 sales 一直没跑过）

### 教训新增

| # | 教训 |
|---|------|
| 17 | 采集脚本与判分器的字段契约要像 API 一样冻结——两边各自演化就是静默 FAIL（B-09 键名漂移之训） |
| 18 | 对外消费面（MCP 与 HTTP 同一数据）返回结构必须一致——外部 agent 不该为接入方式付出不同的解析代码 |

## 第 9 轮：v0.5 配置工作台 + 报表分层 v0 + 契约账本 v2（2026-09-21 夜）

### 交付

- **配置工作台**（v0.5 主交付）：`/api/config/*` 六端点 + 门户工作台页 + 首开 AI 横幅（详见第 7 轮续作）；启动 exe/点火指南同日交付
- **报表分层 v0**：`dashboard.yml` 报表声明 `base: 上级报表`，编译为 mart-on-mart 级联（L1→L5），上级自动携带后代指标可加列、分组维度后代传导；演示账套 `ladder`（集团经营核算·利润阶梯，8 源 9 报表 52 节点全绿，三层级联数值与台账直算 oracle 一致）
- **契约违规统一账本 v2**：校验改两阶段（先全量记录后剔行）；规则词表钉死（type/required/range/enum/format/unique + 匹配契约 fanout/null_match/orphan_right）；匹配契约违规收获进 raw.contract_report（门户链自动 + CLI 走 `semantic.harvest`）——挂起队列自此覆盖匹配契约（v0.3 遗留闭环）
- **字段级血缘修复**：pandas 默认 NA 识别把 'N/A' 静默当缺失（v0.1 起类型违规漏记）+ manifest 无列信息致 SQLGlot 解析失败（v0.3 迁移遗留）——双修后全账套解析成功；mart 生成列元数据 yml（字典/血缘不再 0 字段）
- **v0.6 深度测试**（tests/v0.6，三权分立+密封答案+初见杀）：R1 12/19 → 归因修复 → R2 22/28 → 归因修复 → **R3 26/28（变异 kill 6/6 债务清零，W 路首次全绿）**；闸门 0/3，剩余 2 条失败均非引擎缺陷（B-05 采集工序三轮触发熔断、B-08 为 R2 遗留字节态迁移回声）

### 教训新增

| # | 教训 |
|---|------|
| 19 | **pandas 默认 NA 识别是隐式语义**：'N/A'/'NA' 等被静默当 NaN，类型违规变"缺失"漏记——读文件必须 keep_default_na=False，空串归一显式化 |
| 20 | **先剔行会掩盖同 row 后续违规**：契约校验必须两阶段（先全量记录、后统一执行动作）——顺序依赖是静默错报之母 |
| 21 | **规则词表无文档定义 = 双盲**：命题按文档猜 `required`、引擎写 `required_missing_dropped`，密封测试一轮才撞出来——词表/状态机这类"接口中的接口"必须钉进设计文档 |
| 22 | **变异装备要防等价变异与僵尸服务器**：注入位点必须唯一守门（残留闸=等价变异必然幸存）；探针前必须断言服务器身份（上一轮残留的 8630 僵尸进程让 M1 假幸存） |
| 23 | **修复熔断的正确用法**：B-05 观察键三轮三种死法（丢后缀/丢后缀/回抄词表）——第三轮触发熔断，不再修 tester，升级为采集协议修订（observed 逐字用声明键、原始观察进 evidence） |
| 24 | 门户 save 在 Windows 会把 LF 翻译成 CRLF（text mode 隐式换行）——写配置/生成物必须 newline="" 显式化，杜绝字节级抖动污染哈希验收 |

### 遗留（晨报已列，待用户拍板）

- 层级功能演进方向 + 脚本运行视图形态（见 docs/决策-20260921-层级功能与运行视图.md，三角色头脑风暴+行业调研已完成）
- v0.6 闸门剩余：R4（B-05 协议修订后复跑，预期全绿）→ R5 命题加深 → R6 回归，约 3 小时
- tests/v0.6 三权分立体系运转实证：密封答案两轮重申报零泄漏、变异债务全部清偿、判分器两处自身缺陷由评审自首并钉哈希
